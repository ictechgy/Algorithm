#include <stdio.h>
#include <vector>
using namespace std;

int main(void){
    int n, i, j, tmp;
    scanf("%d", &n);
    vector<int> arr(n);
    for(i = 0; i < n; i++){
        scanf("%d", &arr[i]);
    }
    for(i = 1; i < n; i++){
        for(j = i; j >= 0; j--){

        }
    }

    return 0;
}